<h2>New place</h2>

<%= render "form.html", changeset: @changeset,
                        action: place_path(@conn, :create),
                        categories: @categories %>

<%= link "Back", to: place_path(@conn, :index) %>
